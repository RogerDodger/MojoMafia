% layout 'game';

%= include 'game/pager';

<section class="posts">
	% while (my $post = stash('posts')->next) {
		%= include 'post/single', post => $post;
	% }
</section>

%= include 'game/pager';

% if (defined stash('player') && stash('player')->can_talk) {
	%= include 'post/submit';
% }

